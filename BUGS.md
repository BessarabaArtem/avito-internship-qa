markdown
# Отчет о багах API

Я запустил автотесты и обнаружил, что API работает некорректно. Основная проблема — невозможно создать объявление, из-за чего падают остальные тесты.

| ID | Название проблемы | Приоритет | Описание | Ожидание |
|:---|:---|:---|:---|:---|
| **1** | **Нельзя создать объявление: требует поле "likes"** | 🔴 **Высокий** | При отправке запроса на создание товара (`POST /item`) сервер возвращает ошибку `400` и пишет: *"поле likes обязательно"*. | У нового объявления лайков быть не должно (по умолчанию 0). Сервер не должен требовать это поле при создании. |
| **2** | **Ошибка при передаче статистики вручную** | 🔴 **Высокий** | Я пробовал передать поля `likes`, `contacts`, `viewCount` (и в корне JSON, и внутри объекта statistics), но сервер всё равно возвращает ошибку `400`. | Если передать требуемые поля, объявление должно создаваться. Сейчас создание сломано полностью. |
| **3** | **Тест на отрицательную цену падает с неверной ошибкой** | 🟡 **Средний** | Тест с ценой `-100` падает, но не потому что цена отрицательная, а из-за ошибки с лайками (см. Баг 1). | Я не могу проверить валидацию цены, пока не починят создание объявлений. |
| **4** | **Странный ответ сервера при ошибке** | 🟢 **Низкий** | Вместо нормального JSON с описанием ошибки иногда приходит HTML или странный текст `ID айтема не UUID`, который ломает парсинг в тестах. | При любой ошибке сервер должен возвращать понятный JSON. |