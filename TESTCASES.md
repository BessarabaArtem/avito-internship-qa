```markdown
# 📋 Тест-кейсы API (Microservice)

В этом документе описаны проверки для 4-х методов микросервиса объявлений.

## 1. Метод POST /item (Создание объявления)

| ID | Сценарий | Тестовые данные | Ожидаемый результат |
|:---|:---|:---|:---|
| **TC-1.1** | **Успешное создание (Valid)** | `sellerID`: 112233<br>`name`: "MacBook Pro"<br>`price`: 250000 | **200 OK**. В ответе вернулся уникальный ID, статус сохранения успешный. |
| **TC-1.2** | **Граничное значение SellerID (Min)** | `sellerID`: 111111 | **200 OK**. Объявление создано. |
| **TC-1.3** | **Граничное значение SellerID (Max)** | `sellerID`: 999999 | **200 OK**. Объявление создано. |
| **TC-1.4** | **Цена меньше нуля (Validation)** | `price`: -100 | **400 Bad Request**. Ошибка валидации цены. |
| **TC-1.5** | **Пустое название товара** | `name`: "" | **400 Bad Request**. Ошибка валидации названия. |
| **TC-1.6** | **Некорректный тип данных (String in Price)** | `price`: "free" | **400 Bad Request**. Ошибка типа данных. |

## 2. Метод GET /item/{id} (Получение объявления)

| ID | Сценарий | Шаги | Ожидаемый результат |
|:---|:---|:---|:---|
| **TC-2.1** | **Получение по валидному ID** | 1. Создать объявление.<br>2. Запросить его по ID. | **200 OK**. Поля `name`, `price`, `sellerID` совпадают с созданными. |
| **TC-2.2** | **Запрос несуществующего ID** | 1. Запросить ID `00000000-0000-0000-0000-000000000000` | **404 Not Found**. Сообщение, что элемент не найден. |
| **TC-2.3** | **Невалидный формат ID** | 1. Запросить ID `invalid-id-string` | **400 Bad Request**. |

## 3. Метод GET /{sellerID}/item (Объявления продавца)

| ID | Сценарий | Шаги | Ожидаемый результат |
|:---|:---|:---|:---|
| **TC-3.1** | **Получение списка объявлений** | 1. Создать объявление для `sellerID` 555555.<br>2. Выполнить поиск по этому продавцу. | **200 OK**. Массив содержит созданное объявление. |
| **TC-3.2** | **Продавец без объявлений** | 1. Запросить `sellerID` 999000 (у которого нет товаров). | **200 OK**. Пустой массив `[]`. |
| **TC-3.3** | **Некорректный SellerID** | 1. Запросить `sellerID` "abc". | **400 Bad Request**. |

## 4. Метод GET /statistic/{id} (Статистика)

| ID | Сценарий | Шаги | Ожидаемый результат |
|:---|:---|:---|:---|
| **TC-4.1** | **Получение статистики** | 1. Создать объявление.<br>2. Запросить статистику по ID. | **200 OK**. Поля `likes`, `viewCount`, `contacts` присутствуют и >= 0. |
| **TC-4.2** | **Статистика несуществующего объявления** | 1. Запросить статистику по ID `0000...` | **404 Not Found**. |
```

---